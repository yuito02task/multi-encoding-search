# プロジェクト開発ルール & 自動実行指示

本プロジェクト（Multi-Encoding Search）における開発・修正・機能追加の際は、以下のワークフローを**ユーザーへの確認待ちで中断することなく、Git へのコミット・プッシュまで自動で最後まで完了**させてください。

## リリース・修正の標準作業フロー（自動完遂ルール）

機能追加やバグ修正を行う際は、以下のステップ [1] から [5] までを一貫して自動で実行してください：

1. **コード修正 & 型チェック**
   - `npm run compile` を実行し、TypeScript エラー 0 件を確認する。
2. **動作検証**
   - 変更内容が正しく反映されているかを検証する。
3. **バージョン更新**
   - `package.json`, `README.md`, `DEVELOPMENT_NOTES.md` のバージョン番号を更新する。
   - 小さな修正・バグ修正: パッチ (例: 1.1.0 -> 1.1.1)
   - 新機能の追加: マイナー (例: 1.1.0 -> 1.2.0)
4. **VSIX パッケージの作成 (Marketplace 公開準備)**
   - `npx @vscode/vsce package` を実行し、`.vsix` ファイルを生成する。
5. **Git のステージング・コミット・プッシュ（自動実行）**
   - `git add .`
   - `git commit -m "feat/fix: xxx (vX.X.X)"`
   - `git push origin main`
   - ※ Marketplace へのアップロード作業はユーザーが手動で行うため、Git プッシュ完了時点で完了報告を行う。

## 基本コーディングルール
- コメントとドキュメントは日本語で記述する
- 変数名・関数名は英語のキャメルケース
- 処理ごとにコメントを入れ、何をしているかを明示する
- エラーハンドリングを必ず実装する
- コードは初心者でも読めるようにシンプルに書く
- 複雑な処理は小さい関数に分割する
- 不明な点がある場合のみ事前に質問する
